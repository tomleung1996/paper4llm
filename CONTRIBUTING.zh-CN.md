# 为 Paper for LLMs 做贡献

[English](CONTRIBUTING.md) | 简体中文

感谢你帮助改进 Paper for LLMs。贡献可以包括解析器修复、支持新的出版商页面变体、测试、文档、翻译、无障碍改进或界面优化。

## 开始之前

- 新建 Issue 前请先搜索已有问题。
- 每个 Issue 或 Pull Request 尽量只处理一个问题。
- 可以提供有代表性的公开论文网址，但不要发布账户信息、Cookie、访问令牌、机构凭据或其他私人数据。
- 不要附上完整的受版权保护论文或完整保存的出版商页面。测试材料应缩减为重现问题所需的最小 DOM 片段。
- 不要提交绕过付费墙、登录要求、速率限制或人机验证系统的代码。
- 讨论 Bug 和设计决策时请保持尊重，并默认他人出于善意。

## 开发环境

扩展没有第三方运行时依赖，也不需要构建步骤。你需要当前版本的 Node.js/npm，以及支持 Manifest V3 的 Chrome 或其他 Chromium 浏览器。

运行静态检查和单元测试：

```bash
npm run check
npm test
```

运行浏览器 DOM 集成测试：

```bash
npm run test:browser
```

然后打开 `http://127.0.0.1:4173/tests/browser-test.html`，确认所有断言均通过。

如需测试实际扩展，请打开 `chrome://extensions/`，启用开发者模式，并将仓库根目录作为未打包扩展加载。

## 报告解析器问题

请尽量提供：

- 出版商和代表性论文网址。
- 浏览器版本和扩展版本。
- 预期 Markdown，以及实际 Markdown 中与问题相关的最小片段。
- 问题影响的是元数据、作者、正文、文内引用、参考文献、公式、表格、图片还是脚注。
- 与问题相关时，提供扩展显示的引用完整性统计。
- 在能够合法分享且不含私人信息的前提下，提供截图或最小化 DOM 片段。

请避免发布转换后的完整论文。通常只需要故障位置附近的一小段内容。

## 更新或新增解析器

- 优先使用出版商提供的语义标识符和关系，不根据可见文本猜测。
- 将出版商特定规则放在对应解析器中，将可复用的 Markdown 行为放在 `src/core.js` 中。
- 以确定性方式保留引文和参考文献标识符；目标存在歧义时，不要静默虚构匹配。
- 假设页面结构可能变化。添加范围明确的后备规则，避免选择导航、推荐、指标或无关组件。
- 仅在安全的情况下规范空白；保留有意义的段落、列表、表格、公式和图注边界。
- 保持全部处理在本地进行，不添加分析、追踪、外部服务或远程可执行代码。
- 每个可以用最小化样例表示的解析器 Bug 都应添加回归测试。

## 测试样例政策

测试样例必须是合成内容、公共领域内容、已获复用许可的内容，或缩减到测试所需结构的最小片段。删除与解析行为无关的人名和正文。不得提交凭据、Cookie、下载的付费页面、完整论文文本，或没有明确再分发许可的出版商素材。

## Pull Request 清单

- [ ] 改动范围集中，说明清楚。
- [ ] 所有面向用户的英文和简体中文文本均同步更新。
- [ ] 新增的出版商行为包含适当的回归测试。
- [ ] `npm run check` 和 `npm test` 均通过。
- [ ] 如果改动影响提取或渲染，浏览器 DOM 测试也已通过。
- [ ] 如果数据访问方式发生变化，已同步更新权限和隐私披露。
- [ ] 未误加入私人文件、受版权保护内容、生成的构建文件或本地备份。

提交贡献即表示你同意：你的贡献可以依据本仓库的 [MIT 许可证](LICENSE)进行分发。
